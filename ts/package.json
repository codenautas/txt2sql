{
    "name": "txt-to-sql",
    "description": "txt-to-sql 2.0",
    "version": "2.0.0",
    "private": true,
    "private-why": "it is marked as private to not publish in npm for now",
    "dependencies": {
        "typescript": "^4.8.4"
    },
    "devDependencies": {
        "@types/mocha": "^10.0.0",
        "@types/node": "^18.8.3",
        "@typescript-eslint/eslint-plugin": "^5.39.0",
        "@typescript-eslint/parser": "^5.39.0",
        "eslint": "^8.25.0",
        "js-yaml": "^4.1.0",
        "mocha": "^10.0.0",
        "typescript": "~4.8.4",
        "yaml": "^2.1.3"
    },
    "scripts": {
        "prepare": "tsc -p src",
        "test": "tsc -p test && mocha build-test/test/test*.js",
        "example": "tsc -p src && node build/txt2sql.js ../sandbox/example.tab"
    }
}
